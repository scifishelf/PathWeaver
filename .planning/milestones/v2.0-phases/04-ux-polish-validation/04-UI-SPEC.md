---
phase: 4
slug: ux-polish-validation
status: draft
shadcn_initialized: false
preset: none
created: 2026-03-17
---

# Phase 4 — UI Design Contract

> Visual and interaction contract for Phase 4: UX Polish & Validation.
> Generated by gsd-ui-researcher, verified by gsd-ui-checker.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | none — existing dark glassmorphism theme via inline styles in component files |
| Preset | not applicable |
| Component library | none — custom `Button.tsx` (primary / outline / ghost variants) |
| Icon library | lucide-react — BookOpen, Grid3x3, Keyboard, Zap (already installed) |
| Font | Inter, ui-sans-serif, system-ui (declared in `web/src/index.css` body rule) |

**Rationale:** Phase 4 introduces zero new UI components. The design system is inherited verbatim from Phase 2 (approved 02-UI-SPEC.md). The only user-visible change is a text removal on one line of `HelpOverlay.tsx`. No new tokens, no new color values, no new spacing are introduced. Registry safety gate: not applicable.

**Note on theme divergence:** The approved Phase 2 UI-SPEC describes a light theme (COLOR_BG `#ffffff`). The current codebase runs a dark space/glassmorphism theme (background `#0a0f1e`–`#1a0533` gradient, foreground `#f8fafc`, accent cyan `#22d3ee`). Phase 4 does not resolve this divergence — it operates within the dark theme as currently implemented. The text removal is theme-agnostic.

---

## Spacing Scale

No new spacing introduced in Phase 4. Inheriting scale from 02-UI-SPEC.md.

| Token | Value | Usage |
|-------|-------|-------|
| xs | 4px | Icon gaps, inline padding |
| sm | 8px | Compact element spacing |
| md | 16px | Default element spacing |
| lg | 24px | Section padding |
| xl | 32px | Layout gaps |
| 2xl | 48px | Major section breaks |
| 3xl | 64px | Page-level spacing |

Exceptions: none for this phase. HelpOverlay section 4 retains its existing `paddingTop: 12` (12px — within tolerance of the 8-point scale).

*Source: 02-UI-SPEC.md (inherited, no new values)*

---

## Typography

No new type styles introduced in Phase 4. Inheriting from current codebase.

| Role | Size | Weight | Line Height | Usage |
|------|------|--------|-------------|-------|
| Body | 13px | 400 (regular) | 1.5 | HelpOverlay section text, Modal body |
| Label | 12px | 400 (regular) | 1.4 | HelpOverlay section 4 connections text (the line being edited) |
| Heading | 13px | 600 (semibold) | 1.2 | HelpOverlay section headers (sectionHeader style) |
| Display | 20px | 700 (bold) | 1.2 | Modal title (inherited from Modal.tsx) |

**Precision for the edited line (HelpOverlay.tsx line 132):**
- The connections footer text is rendered at `fontSize: 12, color: rgba(255,255,255,0.5)`
- After the text removal, the same font size and color are retained unchanged
- No new typographic styles are introduced by this edit

*Source: Direct codebase inspection of HelpOverlay.tsx*

---

## Color

No new colors introduced in Phase 4. Inheriting dark theme from current implementation.

| Role | Value | Usage |
|------|-------|-------|
| Dominant (60%) | `#0a0f1e` → `#1a0533` (gradient) | Canvas background, page background |
| Secondary (30%) | `rgba(255,255,255,0.06)` | Glassmorphism card surfaces (.glass), node backgrounds |
| Accent (10%) | `#22d3ee` (cyan) | Critical path nodes, critical path edges, CP indicator in HelpOverlay |
| Accent secondary | `#60a5fa` (blue) | Input focus rings, handles, section header icons |
| Destructive | `#ef4444` (red) | Error node borders (retained from prior phases) |

**Accent reserved for:**
1. Critical path node border and background tint: `rgba(34,211,238,0.08)` bg + `rgba(34,211,238,0.35)` border
2. Critical path edge stroke
3. CP indicator badge in HelpOverlay section 3: `color: #22d3ee`
4. Cyan glow utilities: `.glow-cyan` (`box-shadow: 0 0 24px rgba(34,211,238,0.45)`)

**Accent is NOT used for:**
- The text removal line (connections footer) — it uses `rgba(255,255,255,0.5)` and `#60a5fa` for the BookOpen icon
- Hover states on standard buttons

*Source: Direct codebase inspection of index.css, TaskNode.tsx, HelpOverlay.tsx*

---

## Copywriting Contract

Phase 4 has one user-visible copy change (UX-02) and one code comment (UX-04). No new CTAs, empty states, or destructive actions are introduced.

| Element | Before | After | Requirement |
|---------|--------|-------|-------------|
| HelpOverlay connections footer | `Connect: drag from right handle to target node · Max. 1 outgoing per task` | `Connect: drag from right handle to target node` | UX-02 |
| serialize.ts edge ID comment | (none) | `// Edge IDs use the \`${from}-${to}\` scheme. This assumes each node has a single` / `// source handle (one outgoing connection slot), so the pair (from, to) is unique.` / `// If multi-handle nodes are introduced, this scheme must be revisited.` | UX-04 |

**Standard copywriting elements (inherited, no changes):**

| Element | Copy |
|---------|------|
| Primary CTA | Not applicable — Phase 4 introduces no new CTAs |
| Empty state heading | Not applicable — no new empty states |
| Empty state body | Not applicable |
| Error state | Pre-existing error strings (English) — not modified in Phase 4 |
| Destructive confirmation | Not applicable — no destructive actions in this phase |

**Pre-existing test language mismatch (do NOT fix in Phase 4):**
11 tests expect German error strings (`'Start hat Eingänge'`, `'Zyklus'`, etc.) while implementation uses English. This is pre-existing debt. Phase 4 must not increase the failure count above 11. Documented here for verifier awareness.

*Source: RESEARCH.md Pattern 1 (UX-02 exact text), Pattern 2 (UX-04 exact comment), Pitfall 1 (language mismatch)*

---

## Component Inventory

### `web/src/components/HelpOverlay.tsx` — EDIT (line 132)

**Contract:** Remove ` · Max. 1 outgoing per task` from the connections footer text.

- Before: `Connect: drag from right handle to target node · Max. 1 outgoing per task`
- After: `Connect: drag from right handle to target node`
- All surrounding JSX (`style`, `color`, `fontSize`, `borderTop`, `paddingTop`) is retained unchanged
- The `BookOpen` icon remains in place

Do NOT restructure the JSX. Do NOT change font size, color, or container styles. Single text edit only.

### `web/src/persistence/serialize.ts` — EDIT (above line 69)

**Contract:** Add a 3-line block comment immediately above the edge reconstruction line.

- Insertion point: above `const edges: Edge[] = project.edges.map(...)`
- Comment text (exact):
  ```
  // Edge IDs use the `${from}-${to}` scheme. This assumes each node has a single
  // source handle (one outgoing connection slot), so the pair (from, to) is unique.
  // If multi-handle nodes are introduced, this scheme must be revisited.
  ```
- No other changes to serialize.ts in this task

### `web/src/persistence/serialize.test.ts` — ADD test

**Contract:** Add a `describe('v1.0 backward compatibility (UX-03)', ...)` block at the end of the existing test file.

Test must:
1. Construct an in-memory `ProjectJSON` fixture with `settings.version: '1.0'` and a linear chain (start → A(dur=3) → B(dur=5) → end)
2. Assert `isProjectJSON(v1Project)` returns `true`
3. Assert `fromProjectJSON(v1Project)` does not throw and returns 4 nodes + 3 edges
4. Assert edge `A→B` has `id === 'A-B'`
5. Round-trip through `toProjectJSON` + `computeCPM` and assert `durationAT === 8`, node A `ES === 0`, node B `ES === 3`, both A and B in `criticalNodeIds`

Do NOT add a new test file. Add the describe block to the existing `serialize.test.ts`.

*Source: RESEARCH.md Pattern 3 (exact test fixture and assertions)*

---

## Interaction States

No new interaction states introduced in Phase 4.

The edited HelpOverlay connections footer is read-only text — no hover, focus, or active states to specify.

*Source: Phase scope is text removal + code comment + unit test — zero new interactive elements*

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | none | not applicable — shadcn not initialized |
| lucide-react (npm) | BookOpen (already used in HelpOverlay.tsx) | standard npm dependency — no registry vetting required |

No third-party shadcn registries declared. Registry safety gate: not applicable.

---

## Anti-Patterns (Executor Must Avoid)

1. **Do not remove the BookOpen icon or container div in HelpOverlay.tsx.** Only remove the text fragment ` · Max. 1 outgoing per task`.
2. **Do not change the container styles of the connections footer section.** `paddingTop`, `borderTop`, `fontSize`, `color` are all retained as-is.
3. **Do not fix the pre-existing German/English language mismatch in tests.** Out of scope. Document the 11 pre-existing failures; do not touch them.
4. **Do not change `validateProjectJSON` or add a `version: '2.0'` guard.** The v1.0 version string already satisfies the existing guard.
5. **Do not create a new test file.** Add the backward-compatibility test as a `describe` block inside the existing `serialize.test.ts`.
6. **Do not modify `fromProjectJSON` logic.** The function already handles v1.0 files correctly. The test validates existing behavior.
7. **Do not version-bump the JSON schema.** UX-03 is a test-only requirement.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS
- [ ] Dimension 5 Spacing: PASS
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
