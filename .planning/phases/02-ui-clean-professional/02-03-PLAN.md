---
phase: 02-ui-clean-professional
plan: 03
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - web/src/graph/TaskNode.tsx
  - web/src/graph/StartNode.tsx
  - web/src/graph/EndNode.tsx
  - web/src/components/GraphCanvas.tsx
  - web/src/components/ContextMenu.tsx
autonomous: true
requirements:
  - UI-CRIT-01
  - UI-CRIT-02
  - UI-TOOLBAR-03
  - UI-FOUND-02

must_haves:
  truths:
    - "TaskNode renders with background COLOR_ACCENT_LIGHT and border 2px solid COLOR_ACCENT when data.computed.critical is true"
    - "TaskNode renders with background COLOR_BG and border 2px solid COLOR_BORDER when data.computed.critical is false or undefined"
    - "TaskNode contains no hardcoded hex values — all colors reference theme tokens"
    - "StartNode uses COLOR_BG for background and COLOR_BORDER for border (not CRITICAL_BG)"
    - "EndNode uses COLOR_BG for background, COLOR_BORDER for border, COLOR_TEXT for the date color"
    - "CP banner has background '#eff6ff', color '#1d4ed8', border 1px solid COLOR_ACCENT, format '◆ Kritischer Pfad: {N} Arbeitstage'"
    - "CP banner text contains NO node ID list and NO end date — only the duration number"
    - "ContextMenu buttons have focus-visible:outline-blue-600 class"
  artifacts:
    - path: "web/src/graph/TaskNode.tsx"
      provides: "Token-migrated task node with CP border"
      contains: "COLOR_ACCENT"
    - path: "web/src/graph/StartNode.tsx"
      provides: "Token-migrated start node"
      contains: "COLOR_BG"
    - path: "web/src/graph/EndNode.tsx"
      provides: "Token-migrated end node"
      contains: "COLOR_TEXT"
    - path: "web/src/components/GraphCanvas.tsx"
      provides: "Restyled CP banner"
      contains: "Kritischer Pfad"
    - path: "web/src/components/ContextMenu.tsx"
      provides: "Focus-visible styles on context menu items"
      contains: "focus-visible:outline-blue-600"
  key_links:
    - from: "web/src/graph/TaskNode.tsx"
      to: "web/src/graph/theme.ts"
      via: "import { COLOR_BORDER, COLOR_ACCENT, COLOR_ACCENT_LIGHT, COLOR_BG, SHADOW_SM, RADIUS_MD } from './theme'"
      pattern: "COLOR_ACCENT"
    - from: "web/src/components/GraphCanvas.tsx"
      to: "web/src/graph/theme.ts"
      via: "import { ..., COLOR_ACCENT, RADIUS_MD, SHADOW_SM } from '../graph/theme'"
      pattern: "eff6ff"
---

<objective>
Migrate node components and GraphCanvas to use design tokens; add CP node border highlight; restyle the CP banner; add focus-visible to ContextMenu.

Purpose: Completes the visual redesign of the graph layer. CP nodes are distinguishable by both fill and border. The CP banner is visually distinct from node highlights. All interactive elements have keyboard focus-visible styles.

Output: TaskNode, StartNode, EndNode all use theme tokens; CP banner restyled with locked format; ContextMenu items have focus-visible outline.
</objective>

<execution_context>
@/Users/ansgar.simon/.claude/get-shit-done/workflows/execute-plan.md
@/Users/ansgar.simon/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-ui-clean-professional/02-CONTEXT.md
@.planning/phases/02-ui-clean-professional/02-UI-SPEC.md
@.planning/phases/02-ui-clean-professional/02-01-SUMMARY.md

<interfaces>
<!-- Current state of all files being modified. Executor reads all of these before writing anything. -->

From web/src/graph/theme.ts (after Plan 02-01):
```typescript
export const COLOR_BG = '#ffffff'
export const COLOR_SURFACE = '#f4f4f5'
export const COLOR_BORDER = '#d4d4d8'
export const COLOR_TEXT = '#18181b'
export const COLOR_TEXT_MUTED = '#71717a'
export const COLOR_ACCENT = '#2563eb'
export const COLOR_ACCENT_LIGHT = '#dbeafe'
export const RADIUS_SM = 6
export const RADIUS_MD = 8
export const SHADOW_SM = '0 1px 3px rgba(0,0,0,.12)'
export const SHADOW_MD = '0 4px 12px rgba(0,0,0,.12)'
export const TRANSITION_DEFAULT = 'all 150ms ease'
export const CRITICAL_BG = COLOR_ACCENT_LIGHT
export const CRITICAL_BORDER = COLOR_ACCENT
```

From web/src/graph/TaskNode.tsx (current — key lines):
```typescript
import { CRITICAL_BG } from './theme'
// outer div:
background: data.computed?.critical ? CRITICAL_BG : '#fff',
border: '2px solid #d4d4d8',
borderRadius: 8,
boxShadow: '0 1px 3px rgba(0,0,0,.12)',
// number input:
border: '1px solid #d4d4d8',
borderRadius: 6,
// title input:
border: '1px solid #d4d4d8',
borderRadius: 6,
```

From web/src/graph/StartNode.tsx (current — key lines):
```typescript
import { CRITICAL_BG } from './theme'
background: CRITICAL_BG,                    // BUG: always blue-100, not semantically correct
border: '2px solid #d4d4d8',
borderRadius: 8,
boxShadow: '0 1px 3px rgba(0,0,0,.12)',
// date input:
border: '1px solid #d4d4d8',
borderRadius: 6,
```

From web/src/graph/EndNode.tsx (current — key lines):
```typescript
import { CRITICAL_BG } from './theme'
background: CRITICAL_BG,                    // BUG: always blue-100
border: '2px solid #d4d4d8',
borderRadius: 8,
boxShadow: '0 1px 3px rgba(0,0,0,.12)',
// date text:
color: '#374151'                             // hardcoded gray
```

From web/src/components/GraphCanvas.tsx (current — CP banner block, lines 317-338):
```typescript
import { CRITICAL_BG } from '../graph/theme'
// ...
{errors.length === 0 && cp && (
  <div style={{
    position: 'fixed',
    top: 16,
    left: '50%',
    transform: 'translateX(-50%)',
    zIndex: 10000,
    background: CRITICAL_BG,              // must change to '#eff6ff'
    border: '1px solid #bfdbfe',          // must change to COLOR_ACCENT
    borderRadius: 8,
    padding: '6px 12px',
    fontSize: 12,
    boxShadow: '0 2px 8px rgba(37,99,235,.15)'
  }}>
    <span>
      Kritischer Pfad: {cp.criticalPath.join(' → ')} · Dauer {cp.project.durationAT} AT
      {cp.project.earliestFinishISO ? ` · Ende ${...2-digit year...}` : ''}
    </span>
  </div>
)}
// styledEdges also contains: style.stroke = '#2563eb' — this is fine, it's not a component style
// The edge.style '#ef4444' in styledNodes/styledEdges is error-state and retained as-is
```

From web/src/components/ContextMenu.tsx (current — button elements):
```typescript
<button
  key={idx}
  className="block w-full text-left px-3 py-2 hover:bg-neutral-50 disabled:text-neutral-400"
  onClick={it.onClick}
  disabled={it.disabled}
>
```

Pitfall notes:
- styledNodes in GraphCanvas.tsx sets border: '2px solid #ef4444' on the ReactFlow NODE WRAPPER (n.style) for error nodes — this is SEPARATE from TaskNode's inner div border. Both layers exist independently. Do not remove the error-state border from styledNodes.
- CP banner background must be '#eff6ff' (blue-50), NOT COLOR_ACCENT_LIGHT '#dbeafe'. These are different values by design.
- Banner text color '#1d4ed8' (blue-700) is not a theme token — use the literal value in the inline style.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Token-migrate TaskNode, StartNode, EndNode with CP border</name>
  <files>
    web/src/graph/TaskNode.tsx,
    web/src/graph/StartNode.tsx,
    web/src/graph/EndNode.tsx
  </files>
  <read_first>
    - web/src/graph/TaskNode.tsx (full current file)
    - web/src/graph/StartNode.tsx (full current file)
    - web/src/graph/EndNode.tsx (full current file)
    - web/src/graph/theme.ts (after Plan 02-01 — see interfaces above)
    - .planning/phases/02-ui-clean-professional/02-UI-SPEC.md (Component Inventory → TaskNode, StartNode, EndNode sections)
    - .planning/phases/02-ui-clean-professional/02-RESEARCH.md (Pitfall 2: StartNode/EndNode CRITICAL_BG, Pattern 6: Critical Node Border)
  </read_first>
  <behavior>
    - TaskNode outer div: border is '2px solid COLOR_ACCENT' when data.computed.critical === true
    - TaskNode outer div: border is '2px solid COLOR_BORDER' when data.computed.critical is falsy
    - TaskNode outer div: background is COLOR_ACCENT_LIGHT when critical, COLOR_BG when not
    - TaskNode number input and title input: border is '1px solid COLOR_BORDER', borderRadius is RADIUS_SM
    - TaskNode outer div: boxShadow is SHADOW_SM, borderRadius is RADIUS_MD
    - StartNode outer div: background is COLOR_BG (not CRITICAL_BG)
    - StartNode outer div: border is '2px solid COLOR_BORDER', borderRadius is RADIUS_MD, boxShadow is SHADOW_SM
    - StartNode date input: border is '1px solid COLOR_BORDER', borderRadius is RADIUS_SM
    - EndNode outer div: background is COLOR_BG (not CRITICAL_BG)
    - EndNode outer div: border is '2px solid COLOR_BORDER', borderRadius is RADIUS_MD, boxShadow is SHADOW_SM
    - EndNode date text: color is COLOR_TEXT (not '#374151')
  </behavior>
  <action>
**web/src/graph/TaskNode.tsx — changes:**

1. Replace import line:
   - FROM: `import { CRITICAL_BG } from './theme'`
   - TO: `import { CRITICAL_BG, COLOR_BORDER, COLOR_ACCENT, COLOR_ACCENT_LIGHT, COLOR_BG, SHADOW_SM, RADIUS_MD, RADIUS_SM } from './theme'`

2. Outer div style — replace these three properties:
   - FROM: `background: data.computed?.critical ? CRITICAL_BG : '#fff',`
   - TO: `background: data.computed?.critical ? COLOR_ACCENT_LIGHT : COLOR_BG,`
   - FROM: `border: '2px solid #d4d4d8',`
   - TO: `border: \`2px solid \${data.computed?.critical ? COLOR_ACCENT : COLOR_BORDER}\`,`
   - FROM: `borderRadius: 8,`
   - TO: `borderRadius: RADIUS_MD,`
   - FROM: `boxShadow: '0 1px 3px rgba(0,0,0,.12)',`
   - TO: `boxShadow: SHADOW_SM,`

3. Number input style — replace:
   - FROM: `border: '1px solid #d4d4d8',`
   - TO: `border: \`1px solid \${COLOR_BORDER}\`,`
   - FROM: `borderRadius: 6,`
   - TO: `borderRadius: RADIUS_SM,`

4. Title input style — replace:
   - FROM: `border: '1px solid #d4d4d8',`
   - TO: `border: \`1px solid \${COLOR_BORDER}\`,`
   - FROM: `borderRadius: 6,`
   - TO: `borderRadius: RADIUS_SM,`

---

**web/src/graph/StartNode.tsx — changes:**

1. Replace import line:
   - FROM: `import { CRITICAL_BG } from './theme'`
   - TO: `import { COLOR_BG, COLOR_BORDER, SHADOW_SM, RADIUS_MD, RADIUS_SM } from './theme'`

2. Outer div style — replace:
   - FROM: `background: CRITICAL_BG,`
   - TO: `background: COLOR_BG,`
   - FROM: `border: '2px solid #d4d4d8',`
   - TO: `border: \`2px solid \${COLOR_BORDER}\`,`
   - FROM: `borderRadius: 8,`
   - TO: `borderRadius: RADIUS_MD,`
   - FROM: `boxShadow: '0 1px 3px rgba(0,0,0,.12)',`
   - TO: `boxShadow: SHADOW_SM,`

3. Date input style — replace:
   - FROM: `border: '1px solid #d4d4d8',`
   - TO: `border: \`1px solid \${COLOR_BORDER}\`,`
   - FROM: `borderRadius: 6,`
   - TO: `borderRadius: RADIUS_SM,`

---

**web/src/graph/EndNode.tsx — changes:**

1. Replace import line:
   - FROM: `import { CRITICAL_BG } from './theme'`
   - TO: `import { COLOR_BG, COLOR_BORDER, COLOR_TEXT, SHADOW_SM, RADIUS_MD } from './theme'`

2. Outer div style — replace:
   - FROM: `background: CRITICAL_BG,`
   - TO: `background: COLOR_BG,`
   - FROM: `border: '2px solid #d4d4d8',`
   - TO: `border: \`2px solid \${COLOR_BORDER}\`,`
   - FROM: `borderRadius: 8,`
   - TO: `borderRadius: RADIUS_MD,`
   - FROM: `boxShadow: '0 1px 3px rgba(0,0,0,.12)',`
   - TO: `boxShadow: SHADOW_SM,`

3. Date text span style — replace:
   - FROM: `color: '#374151'`
   - TO: `color: COLOR_TEXT`
  </action>
  <verify>
    <automated>cd /Users/ansgar.simon/Desktop/coding/PathWeaver/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CRITICAL_BG" web/src/graph/TaskNode.tsx` returns NO match (token replaced by COLOR_ACCENT_LIGHT)
    - `grep "CRITICAL_BG" web/src/graph/StartNode.tsx` returns NO match
    - `grep "CRITICAL_BG" web/src/graph/EndNode.tsx` returns NO match
    - `grep "COLOR_ACCENT" web/src/graph/TaskNode.tsx` returns a match (CP border uses COLOR_ACCENT)
    - `grep "COLOR_BG" web/src/graph/StartNode.tsx` returns a match
    - `grep "COLOR_BG" web/src/graph/EndNode.tsx` returns a match
    - `grep "#d4d4d8" web/src/graph/TaskNode.tsx` returns NO match
    - `grep "#d4d4d8" web/src/graph/StartNode.tsx` returns NO match
    - `grep "#d4d4d8" web/src/graph/EndNode.tsx` returns NO match
    - `grep "#374151" web/src/graph/EndNode.tsx` returns NO match
    - `cd web && npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All three node files use theme tokens; no hardcoded hex values remain; TaskNode has conditional CP border; StartNode and EndNode have COLOR_BG background instead of CRITICAL_BG</done>
</task>

<task type="auto">
  <name>Task 2: Restyle CP banner in GraphCanvas.tsx and add ContextMenu focus-visible</name>
  <files>
    web/src/components/GraphCanvas.tsx,
    web/src/components/ContextMenu.tsx
  </files>
  <read_first>
    - web/src/components/GraphCanvas.tsx (full current file — especially lines 14, 317-338)
    - web/src/components/ContextMenu.tsx (full current file)
    - web/src/graph/theme.ts (after Plan 02-01)
    - .planning/phases/02-ui-clean-professional/02-UI-SPEC.md (Component Inventory → GraphCanvas and ContextMenu sections)
    - .planning/phases/02-ui-clean-professional/02-RESEARCH.md (Pattern 5: CP Banner, Pitfall 3: styledNodes error border is separate layer, Pitfall 6: banner format)
  </read_first>
  <action>
**web/src/components/GraphCanvas.tsx — two changes:**

**Change 1 — Update the import from theme:**

Find the existing import line:
```typescript
import { CRITICAL_BG } from '../graph/theme'
```

Replace with:
```typescript
import { COLOR_ACCENT, RADIUS_MD, SHADOW_SM } from '../graph/theme'
```

(CRITICAL_BG is no longer used in GraphCanvas after this change; the new tokens are used in the banner.)

**Change 2 — Replace the CP banner block (lines 317-338):**

Find the entire block:
```typescript
{errors.length === 0 && cp && (
  <div
    style={{
      position: 'fixed',
      top: 16,
      left: '50%',
      transform: 'translateX(-50%)',
      zIndex: 10000,
      background: CRITICAL_BG,
      border: '1px solid #bfdbfe',
      borderRadius: 8,
      padding: '6px 12px',
      fontSize: 12,
      boxShadow: '0 2px 8px rgba(37,99,235,.15)'
    }}
  >
    <span>
      Kritischer Pfad: {cp.criticalPath.join(' → ')} · Dauer {cp.project.durationAT} AT
      {cp.project.earliestFinishISO ? ` · Ende ${cp.project.earliestFinishISO.slice(8,10)}.${cp.project.earliestFinishISO.slice(5,7)}.${cp.project.earliestFinishISO.slice(2,4)}` : ''}
    </span>
  </div>
)}
```

Replace with:
```typescript
{errors.length === 0 && cp && (
  <div
    style={{
      position: 'fixed',
      top: 16,
      left: '50%',
      transform: 'translateX(-50%)',
      zIndex: 10000,
      background: '#eff6ff',
      color: '#1d4ed8',
      border: `1px solid ${COLOR_ACCENT}`,
      borderRadius: RADIUS_MD,
      padding: '8px 16px',
      fontSize: 12,
      fontWeight: 400,
      boxShadow: SHADOW_SM,
    }}
  >
    ◆ Kritischer Pfad: {cp.project.durationAT} Arbeitstage
  </div>
)}
```

Changes from original:
- `background`: `CRITICAL_BG` ('#dbeafe') → `'#eff6ff'` (blue-50, distinct from node fill)
- `color`: not set before → `'#1d4ed8'` (blue-700)
- `border`: `'1px solid #bfdbfe'` → `\`1px solid \${COLOR_ACCENT}\``
- `borderRadius`: `8` → `RADIUS_MD` (same value, now uses token)
- `padding`: `'6px 12px'` → `'8px 16px'`
- `fontWeight`: not set before → `400`
- `boxShadow`: bespoke rgba → `SHADOW_SM`
- Banner content: `Kritischer Pfad: {cp.criticalPath.join(' → ')} · Dauer {cp.project.durationAT} AT {end date}` → `◆ Kritischer Pfad: {cp.project.durationAT} Arbeitstage`
  - Removes: path node IDs, bullet separator, end date with 2-digit year
  - Adds: diamond bullet ◆, uses full German word "Arbeitstage"

Do NOT touch: styledNodes error-state border (`border: '2px solid #ef4444'`), styledEdges CP stroke (`style.stroke = '#2563eb'`), or the error banner block. Those are intentionally retained.

---

**web/src/components/ContextMenu.tsx — one change:**

Find the button element:
```typescript
<button
  key={idx}
  className="block w-full text-left px-3 py-2 hover:bg-neutral-50 disabled:text-neutral-400"
  onClick={it.onClick}
  disabled={it.disabled}
>
```

Replace with:
```typescript
<button
  key={idx}
  className="block w-full text-left px-3 py-2 hover:bg-neutral-50 disabled:text-neutral-400 focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600"
  onClick={it.onClick}
  disabled={it.disabled}
>
```

Only change: append `focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-blue-600` to the className string.
  </action>
  <verify>
    <automated>cd /Users/ansgar.simon/Desktop/coding/PathWeaver/web && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "CRITICAL_BG" web/src/components/GraphCanvas.tsx` returns NO match
    - `grep "eff6ff" web/src/components/GraphCanvas.tsx` returns a match (banner bg)
    - `grep "1d4ed8" web/src/components/GraphCanvas.tsx` returns a match (banner text color)
    - `grep "COLOR_ACCENT" web/src/components/GraphCanvas.tsx` returns a match (banner border)
    - `grep "Arbeitstage" web/src/components/GraphCanvas.tsx` returns a match (locked banner format)
    - `grep "criticalPath.join" web/src/components/GraphCanvas.tsx` returns NO match (path node IDs removed from banner)
    - `grep "earliestFinishISO" web/src/components/GraphCanvas.tsx` returns NO match (end date removed from banner)
    - `grep "focus-visible:outline-blue-600" web/src/components/ContextMenu.tsx` returns a match
    - `cd web && npx tsc --noEmit` exits 0
    - `cd web && npx vitest run` exits 0
  </acceptance_criteria>
  <done>CP banner uses #eff6ff background with blue-700 text and shows only duration in locked format; GraphCanvas no longer imports CRITICAL_BG; ContextMenu buttons have focus-visible outline</done>
</task>

</tasks>

<verification>
After both tasks:
1. `cd web && npx vitest run` — full suite green
2. `grep -rn "#d4d4d8\|#dbeafe\|#374151\|CRITICAL_BG" web/src/graph/TaskNode.tsx web/src/graph/StartNode.tsx web/src/graph/EndNode.tsx` — zero matches
3. `grep "Arbeitstage" web/src/components/GraphCanvas.tsx` — returns match with correct format
</verification>

<success_criteria>
- TaskNode, StartNode, EndNode have no hardcoded hex values — all from theme tokens
- CP nodes have colored border (COLOR_ACCENT) in addition to background fill (COLOR_ACCENT_LIGHT)
- Start and End nodes use COLOR_BG background (not CRITICAL_BG)
- CP banner: background '#eff6ff', color '#1d4ed8', format '◆ Kritischer Pfad: {N} Arbeitstage'
- ContextMenu buttons have focus-visible:outline-blue-600
- TypeScript compiles without errors
- Full Vitest suite green
</success_criteria>

<output>
After completion, create `.planning/phases/02-ui-clean-professional/02-03-SUMMARY.md`
</output>
